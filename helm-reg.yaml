
projectedSecret:
  - secret:
      name: "properties-2021-06-07"

image:
  tag: unstable-reg

env:
  APP_ENV: reg

ingress:
  enabled: true
  hosts:
    - host: "reports-reg.apps-test.iu.edu"
      paths: []

activeProfiles: "redis,reg"

serviceAccountName: reports-reg

cluster: es-test